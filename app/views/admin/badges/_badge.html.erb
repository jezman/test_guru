<tr>
  <td><%= image_tag badge.image_path, size: "32x32"%></td>
  <td><%= badge.title %></td>
  <td>#todo: rule</td>
  <td><%= link_to t('.show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('.edit'), edit_admin_badge_path(badge) %></td>
  <td>
    <%= link_to t('.delete'),
                admin_badge_path(badge),
                method: :delete,
                data: { confirm: t('.delete_confirmation') } %>
  </td>
</tr>
